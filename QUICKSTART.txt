AUDIO PRIORITY MANAGER - QUICK START GUIDE
==========================================

🎯 PURPOSE:
Automatically lower the volume of other applications when your priority application is playing audio.

📦 INSTALLATION:
1. Ensure Python 3.8+ is installed
2. Run: quick_setup.bat (or pip install -r requirements.txt)
3. Launch the application

🚀 USAGE:

=== GUI MODE (Recommended) ===
Run: python app.py

1. Enter priority process name (e.g., vlc.exe, spotify.exe)
2. Configure duck volume level (0.0-1.0)
3. Click "Start Ducking"
4. Minimize to system tray for background operation

=== CLI MODE ===
Run: python app.py --priority "vlc.exe"

Detailed examples:
- python app.py --priority "vlc.exe" --duck-to 0.2
- python app.py --priority "spotify.exe" --other "chrome.exe" "discord.exe"

🔧 BUILD EXECUTABLE:
Run: python build_exe.py
Creates standalone EXE files in dist/ folder

📁 FILE STRUCTURE:

main.py           - Original file (legacy CLI)
app.py            - Main launcher (GUI + CLI)
src/
  ├─ audio_engine.py - Audio ducking logic  
  ├─ gui.py         - PyQt6 interface
  └─ process_*.py   - Process management
build_exe.py      - EXE build script
assets/
  ├─ logo.ico       - Application icon
  └─ logo.svg       - Vector logo
quick_setup.bat   - Quick installation

🎮 USE CASES:

Gaming:     Duck music when game plays audio
Calls:      Mute background audio during meetings
Media:      Lower notifications while watching movies
Streaming:  Manage multiple audio sources

💡 TIPS:

- Run as Administrator for full audio session access
- Process names must include .exe extension
- You can target multiple specific processes
- Use system tray for background operation
- Configuration is automatically saved
- Check process names in Task Manager if unsure

🐛 TROUBLESHOOTING:

pycaw import error:     pip install pycaw comtypes
PyQt6 import error:     pip install PyQt6  
Audio not detected:     Run as Administrator
Build EXE error:        pip install pyinstaller
Logo not showing:       Check assets/logo.ico exists
Permission denied:      Close running instances before rebuild

📞 SUPPORT:

Check README.md for detailed information
Run the GUI for an interactive experience
All settings are saved automatically
